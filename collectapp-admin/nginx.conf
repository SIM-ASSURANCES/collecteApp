server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Résolveur DNS interne Docker — re-résout le backend à chaque requête
    # (sinon nginx garde l'ancienne IP après un redéploiement du backend → 502)
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $backend_upstream collecte-backend;

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy vers le backend
    location /api/ {
        proxy_pass http://$backend_upstream:5000$request_uri;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # SSE (stats/events)
    location /api/stats/events {
        proxy_pass http://$backend_upstream:5000/api/stats/events;
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding on;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
}
